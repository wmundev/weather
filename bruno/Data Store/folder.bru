meta {
  name: Data Store
  seq: 15
}

docs {
  DynamoDB-backed endpoints - `EmailController` and `MusicController`. Both need
  the DynamoDB tables provisioned (`weather-infra/dynamodb.tf`, or the local
  container under `database/dynamodb/`).
}
