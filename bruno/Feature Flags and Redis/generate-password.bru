meta {
  name: Generate Password
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/feature/password
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

docs {
  `GET /feature/password`

  Placeholder - the generator is still a TODO in `NewFeatureController`, so this
  currently returns an empty `200`.
}
