meta {
  name: Text Utilities
  seq: 10
}

docs {
  Pure string helpers with no external dependencies - `EnglishWordController`
  and `StringBase64Controller`. Handy as smoke tests, since they exercise the
  pipeline without needing DynamoDB, Redis or OpenWeatherMap.
}
